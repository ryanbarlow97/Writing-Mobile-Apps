<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ItemDetailsActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateAccountActivity"
            android:label="Create Account"
            android:theme="@style/AppTheme" />

        <activity android:name=".AddItemActivity"
            android:label="Add Item"
            android:theme="@style/AppTheme" />

        <activity android:name=".EditItemActivity"
            android:label="Edit Item"
            android:theme="@style/AppTheme" />
        <activity android:name=".BlogPostDetailActivity"
            android:label="Blog Post Detail"
            android:theme="@style/AppTheme" />

        <activity android:name=".CreateBlogPostActivity"
            android:label="Create Blog Post"
            android:theme="@style/AppTheme" />

        <activity android:name=".PendingItemsActivity"
            android:label="Pending Items"
            android:theme="@style/AppTheme" />

    </application>
</manifest>
